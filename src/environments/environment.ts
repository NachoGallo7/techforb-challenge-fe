const API_URL = "https://techforb-challenge.onrender.com"

export const environment = {
  production: true,
  apis: {
    countries: `${API_URL}/countries`,
    plants: `${API_URL}/plants`,
    users: `${API_URL}/users`
  }
};
